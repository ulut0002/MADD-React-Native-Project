import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  input: {
    height: 40,
    // margin: 12,
    borderWidth: 1,
    padding: 10,
  },

  inputLabel: {
    fontWeight: "600",
    fontSize: 16,
  },

  inputContainer: {
    flexDirection: "column",
    gap: 4,
    margin: 12,
  },
});
